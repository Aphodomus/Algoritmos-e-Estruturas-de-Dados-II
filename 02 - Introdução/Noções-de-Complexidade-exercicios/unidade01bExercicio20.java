//   Calcule o número de multiplicações que o código abaixo realiza:

public class unidade01bExercicio20 {
    public static void main(String[] args) {
        for (int i = n + 1; i > 0; i /= 2)
            a *= 2;
    }
}

/**
 * O código acima realiza PISO log2(n + 1) + 1 multiplicações.
 * 
 * Complexidade: O(log2(n + 1)), Ω(log2(n + 1)) e Θ(log2(n + 1))
 */
